[workspace]
members = [
    "crates/artemis-core",
]
resolver = "2"

[workspace.dependencies]
##alloy
alloy = { version = "1.4", features = ["full", "providers", "rpc"] }
alloy-node-bindings = { version = "1.4" }

[profile.release]
panic = 'abort'

[profile.dev]
panic = 'abort'
